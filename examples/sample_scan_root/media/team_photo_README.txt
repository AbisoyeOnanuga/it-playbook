# Placeholder image — synthetic 800x600 PNG would live here in a real repo.
# This text stub keeps the sample tree realistic without binary blobs.
# Category: Images | Size represented in scan: ~2.4 MB (simulated via padding file below)
